# 心潮服务器网页版 v0.3 AI 骨架版

## 目标

新增 AI 洞察接入骨架，但默认关闭，不调用真实 AI，继续使用 mock fallback。

## 已完成

- 新增 `backend/src/ai/provider.js`
- 新增 `backend/src/ai/prompt.js`
- 新增 `backend/src/ai/validateInsight.js`
- 新增 `backend/src/ai/insightService.js`
- `server.js` 改为统一调用 `generateInsight`
- `AI_ENABLED=false` 默认走 mock
- `AI_ENABLED=true` 但缺少 key/base/model 时 fallback mock
- 本地 crisis 检测先行
- `validateInsight` 校验 AI JSON 结构
- `ai-fallback-test` 覆盖 AI 关闭、配置缺失、本地 crisis
- smoke test 通过
- persistence test 通过
- AI fallback test 通过

## 当前默认行为

- 不调用真实 AI。
- 不发送 transcript 到第三方。
- 不需要 API key。
- 不影响前端。
- 不改变接口契约。
- 不改变 Ritual 流程。

## 当前限制

- provider 还没有真实网络调用。
- `AI_TIMEOUT_MS` 已预留，但真实 timeout 未实现。
- `node:sqlite` 仍有 experimental warning。
- 正式上线隐私口径未调整。
- 未做生产部署。
- 未决定是否默认启用 AI。

## 验收结果

以下测试均已通过：

- `node --check`
- `npm run smoke`
- `node scripts/persistence-test.js`
- `npm run ai:fallback`

## 下一步建议

- 实现真实 OpenAI compatible provider。
- 继续保持 `AI_ENABLED=false` 默认关闭。
- 增加真实 provider 超时和 JSON 解析防护。
- 更新正式隐私文案。
- 手动验证 AI 文案是否适配现有卡片 UI。
- 决定是否保留 `meta.source` 作为内部调试字段。

## 说明

本文档未写入真实 API key，也未写入真实用户 transcript。
