# 心潮服务器网页版 v0.1 联调版

## 已完成

- 零依赖后端
- 静态前端托管
- 健康检查
- 生成洞察 mock
- 内存 session 保存
- 历史查询
- 7 天 / 30 天模式统计
- 删除数据
- 用户反馈接口
- 前端 API 客户端
- 后端联调开关
- 浏览器语音识别 transcript
- 真实语音到后端洞察闭环

## 人工验证记录

使用语音：

```text
最近不知道方向该怎么选感觉不太清楚
```

返回结果：

```text
emotionLabel = 迷茫
tags = 方向不清 / 不确定性
summary = 这一周，你反复提到不确定性和方向不清。
```

## 当前限制

- 数据存在内存，服务重启丢失。
- 洞察仍是 mock 规则，不是 AI。
- Web Speech API 浏览器兼容性不稳定。
- 隐私文案上线前必须重新确认。
- 默认仍不启用后端，需要 `window.XINHU_USE_BACKEND = true`。

## 下一阶段建议

- 接持久化数据库。
- 接 AI 洞察生成。
- 调整正式隐私口径。
- 决定是否默认启用后端模式。
- 增加正式错误提示策略，但不得破坏现有 UI。
