# 心潮服务器网页版后端大纲

## 文档状态

文档名称：心潮服务器网页版后端大纲  
版本：v0.1  
适用范围：服务器网页版  
前端依据：现有心湖高保真前端 UI 与 Ritual 会话流程  
核心原则：前端 UI 已定稿，后端只补真实能力，不私自改动页面结构、视觉、文案、按钮、流程和交互节奏。

## 一、总原则

### 1. 前端 UI 是唯一显示依据

后端开发必须严格围绕当前前端 UI 服务。

后端不得私自新增以下内容：

* 新页面
* 新按钮
* 新弹窗
* 新登录入口
* 新支付入口
* 新分享入口
* 新社区入口
* 新咨询入口
* 新聊天机器人入口
* 新的结果卡片类型
* 新的用户操作路径
* 新的强制注册流程

后端允许做的事情只有三类：

* 给现有 UI 提供真实数据
* 保存现有 UI 产生的会话记录
* 支撑现有 UI 已经表达过的长期心湖能力

### 2. 不改变 Ritual 四阶段

现有前端会话流程固定为：

```text
入静
倾诉
聆听
洞察
```

后端不能改变这个流程，也不能插入额外步骤。

现有阶段含义保持如下：

```text
入静：用户准备进入状态
倾诉：用户说出当前状态，最长 60 秒
聆听：系统处理刚才的表达
洞察：展示三份洞察卡片
```

### 3. 不改变三份洞察结构

洞察结果固定为三张卡片：

```text
一 · 你真正在纠结的
二 · 我注意到的模式
三 · 带走的问题
```

后端返回内容必须严格填入这三张卡片。

后端不得新增第四张卡片，也不得改变三张卡片标题。

### 4. 不改变现有文案语气

产品气质固定为：

```text
安静
克制
私密
柔和
像湖面显影
像一面镜子
```

后端生成的内容必须符合这个气质。

禁止生成以下风格：

* 鸡汤式安慰
* 夸张鼓励
* 心理诊断
* 医疗判断
* 命令式建议
* 说教式话术
* 咨询师式强干预
* 娱乐测试式结论
* 过度玄学化表达

### 5. 隐私承诺属于产品级约束

当前前端 UI 中有以下隐私表达：

```text
数据不离开你的设备
设备端处理，绝对隐私
语音转录在你的设备本地完成，不上传服务器
所有会话数据在设备上加密存储
```

服务器网页版会涉及后端能力接入，因此这一部分存在产品口径风险。

后端开发不得私自修改前端文案。  
上线前必须由产品负责人统一确认隐私口径。

可执行边界为：

```text
内测阶段可以先接后端能力
上线阶段必须保证隐私文案与真实数据流一致
```

若最终选择服务器保存历史，则前端隐私说明需要由产品负责人统一调整，后端不得单独处理。

## 二、服务器网页版的产品目标

### 1. 当前版本目标

服务器网页版的目标是把现有高保真前端从演示体验升级为真实可用产品。

核心闭环：

```text
用户打开网页
点击开始会话
完成 60 秒以内倾诉
进入聆听阶段
后端生成三份真实洞察
前端展示三张洞察卡片
本次会话存入心湖
用户后续可以看到自己的重复模式
```

### 2. 当前版本不追求的目标

当前阶段不做以下内容：

* App
* 社区
* 会员
* 付费
* 心理咨询
* 人工客服
* 复杂后台
* 多角色陪伴
* 公开广场
* 用户之间互动
* 长篇报告生成
* 医疗级风险评估

### 3. 第一版验证重点

第一版只验证五件事：

```text
用户愿不愿意点开始
用户愿不愿意说出真实内容
三份洞察是否有击中感
用户是否愿意保存到心湖
用户是否愿意几天后再次回来
```

## 三、前端 UI 对应的后端能力

### 1. 营销首页

当前前端结构：

```text
导航
Hero
信任条
如何运作
核心功能
隐私承诺
用户心声
疗愈路径
Footer
```

后端要求：

* 不干预首页视觉
* 不新增首页模块
* 不改变首页文案
* 不改变开始会话按钮
* 不改变导航逻辑
* 只保证开始会话后可以进入真实后端链路

首页中涉及长期能力的展示内容，后端需要逐步支撑：

```text
7 天后开始看见规律
30 天后认识全新的自己
思维模式追踪
情绪可视化
本次会话存入你的心湖
```

### 2. Ritual 会话覆盖层

当前前端入口：

```text
window.Ritual.open()
```

后端要求：

* 不改变入口方式
* 不改变全屏覆盖层形式
* 不改变顶部品牌“心湖会话”
* 不改变关闭按钮
* 不改变进度点样式
* 不改变四阶段推进方式
* 不改变 60 秒限制
* 不改变“说完了”按钮
* 不改变洞察卡片布局

### 3. 入静阶段

前端显示内容：

```text
入静
先陪自己，深呼吸三次
跟随这颗光球，吸气时它舒展，呼气时它收拢。
当你觉得安定了，就开始说。
我准备好了，开始说
```

后端要求：

* 入静阶段不请求后端
* 不记录用户行为细节
* 不改变入静时长
* 不改变呼吸光球逻辑

### 4. 倾诉阶段

前端显示内容：

```text
倾诉
今天，什么在你心里？
不用组织语言，想到什么就说什么
就让它自然地流出来…
0:00 / 1:00
说完了
```

后端要求：

* 倾诉阶段负责接收最终文本结果
* 不要求前端改变声波动画
* 不要求前端改变倒计时环
* 不要求前端显示转写过程
* 不在倾诉阶段打断用户
* 不在倾诉阶段弹出错误提示

输入到后端的核心内容：

```text
本次倾诉文本
本次倾诉时长
匿名用户标识
会话发生时间
```

### 5. 聆听阶段

前端显示内容：

```text
正在听见你…
理解你说出的话
寻找话语之下的东西
准备三面小小的镜子
```

后端要求：

* 后端请求应发生在聆听阶段
* 前端保持现有聆听视觉
* 不新增普通加载动画
* 不新增进度条
* 不改变 4.6 秒的基本感受
* 后端慢时允许前端继续保持聆听状态
* 后端失败时返回可控错误，由前端决定降级展示

### 6. 洞察阶段

前端显示内容：

```text
三份洞察
这是此刻的你，在湖面上的倒影
一 · 你真正在纠结的
二 · 我注意到的模式
三 · 带走的问题
本次会话已存入你的心湖 · 仅你可见
再说一次
把问题带走，完成
```

后端要求：

* 返回三份洞察
* 返回保存状态
* 返回本次会话标识
* 返回情绪标签
* 返回主题标签
* 返回风险级别
* 不新增结果区块
* 不改变按钮含义
* 不改变“再说一次”逻辑
* 不改变“完成”逻辑

## 四、第一版后端范围

### 1. 必须完成

第一版必须完成以下能力：

```text
匿名用户识别
接收倾诉文本
生成三份洞察
保存会话记录
返回保存状态
查询历史会话
查询 7 天模式
查询 30 天模式
删除单条记录
删除全部记录
记录用户反馈
基础安全检测
基础频率限制
```

### 2. 可以预留

以下能力可以先设计数据位置，前端未接入前不显示：

```text
收藏问题
导出记录
每周总结
每月总结
情绪趋势
高频主题统计
反馈质量分析
```

### 3. 暂不开发

以下内容第一版不开发：

```text
正式账号系统
手机号登录
微信登录
邮箱登录
会员订阅
支付系统
社区系统
人工咨询
多端同步
复杂运营后台
用户画像后台
公开分享页
```

## 五、数据对象大纲

### 1. 匿名用户

用途：让用户无需注册也能保存自己的心湖记录。

需要记录：

```text
匿名用户标识
首次访问时间
最近访问时间
数据是否已删除
历史保存开关
```

### 2. 会话记录

用途：保存用户每一次完整倾诉后的结果。

需要记录：

```text
会话标识
匿名用户标识
倾诉文本
倾诉时长
你真正在纠结的
我注意到的模式
带走的问题
情绪标签
主题标签
风险级别
创建时间
是否删除
是否收藏
用户反馈
```

### 3. 情绪标签

用途：用于 7 天和 30 天模式追踪。

示例：

```text
焦虑
疲惫
委屈
愤怒
迷茫
压力
孤独
平静
混合
```

### 4. 主题标签

用途：识别用户反复出现的内在主题。

示例：

```text
不确定性
失控感
自我怀疑
关系压力
期待压力
自我压抑
疲惫感
方向不清
独自承担
过度分析
```

### 5. 模式总结

用途：支撑首页中已经展示过的长期心湖能力。

需要支持：

```text
最近 7 天高频情绪
最近 7 天高频主题
最近 30 天高频情绪
最近 30 天高频主题
重复出现的问题
本周一句总结
本月一句总结
```

## 六、核心结果格式

### 1. 洞察结果必须短

每张卡片只允许一句主要内容。

推荐长度：

```text
你真正在纠结的：24 到 48 个中文字符
我注意到的模式：24 到 60 个中文字符
带走的问题：18 到 42 个中文字符
```

不得输出长段解释。  
不得输出多条建议。  
不得输出编号列表。  
不得让卡片撑破现有 UI。

### 2. 洞察结果必须适合当前卡片

第一张卡片负责指出核心卡点：

```text
用户表面上在说什么
底层真正卡住的是什么
```

第二张卡片负责指出重复模式：

```text
用户如何表达压力
用户如何处理关系
用户如何对待自己
用户如何回避某个问题
```

第三张卡片负责留下一个问题：

```text
开放式
可带走
不命令
不评判
不替用户做决定
```

### 3. 洞察语言标准

允许：

```text
你表面上在说累，底层更像是在承受持续的期待压力。
你多次把注意力放在别人需要什么，较少表达自己真正想要什么。
今天有没有一件事，可以只按你的真实需要来安排？
```

禁止：

```text
你有严重焦虑症。
你必须马上改变自己。
你一定要离开这段关系。
你太敏感了。
你需要接受专业治疗。
你的人生会越来越好。
```

## 七、接口大纲

### 1. 生成洞察

用途：支撑 Ritual 聆听阶段到洞察阶段。

请求内容：

```json
{
  "anonymousId": "匿名用户标识",
  "transcript": "用户本次倾诉文本",
  "durationSeconds": 42,
  "timezone": "Asia/Shanghai"
}
```

返回内容：

```json
{
  "ok": true,
  "data": {
    "sessionId": "本次会话标识",
    "insight": {
      "struggle": "你真正在纠结的内容",
      "pattern": "我注意到的模式内容",
      "question": "带走的问题内容"
    },
    "meta": {
      "emotionLabel": "疲惫",
      "tags": [
        {
          "tag": "期待压力",
          "score": 0.84
        },
        {
          "tag": "自我压抑",
          "score": 0.78
        }
      ],
      "riskLevel": "normal",
      "saved": true
    }
  },
  "error": null
}
```

### 2. 查询历史会话

用途：支撑我的心湖历史记录。

返回内容必须包含：

```text
会话标识
创建时间
三份洞察
情绪标签
主题标签
是否收藏
用户反馈
```

### 3. 查询 7 天模式

用途：支撑短期心绪规律。

返回内容必须包含：

```text
会话数量
高频情绪
高频主题
本周重复问题
本周一句总结
```

### 4. 查询 30 天模式

用途：支撑第 30 天心湖。

返回内容必须包含：

```text
会话数量
高频情绪
高频主题
常见触发场景
重复表达模式
本月一句总结
带走的问题
```

### 5. 删除数据

用途：支撑隐私承诺。

必须支持：

```text
删除单次会话
删除全部会话
关闭历史保存
```

删除后要求：

```text
前端不再展示对应历史
模式统计不再包含已删除内容
用户可重新开始新会话
```

### 6. 用户反馈

用途：判断洞察质量。

反馈选项：

```text
准
有一点准
不准
```

可选补充：

```text
哪张卡最有用
哪张卡没有说到点上
用户希望更直接还是更温和
```

## 八、安全与风险大纲

### 1. 必须识别的风险

后端需要识别以下内容：

```text
自伤表达
自杀表达
伤害他人表达
现实暴力风险
极端崩溃表达
明显求救表达
```

### 2. 普通内容处理

普通内容正常生成三份洞察。

返回：

```text
riskLevel = normal
```

### 3. 较高风险内容处理

较高风险内容可以返回温和安全提醒，同时保留克制表达。

返回：

```text
riskLevel = elevated
```

### 4. 危机内容处理

危机内容不展示普通三份洞察。

返回：

```text
riskLevel = crisis
safetyMessage = 安全提示内容
```

前端如何展示由产品另行确认。后端不得私自新增危机弹窗 UI。

## 九、异常状态大纲

### 1. 文本为空

场景：用户没有说话，或转写失败。

后端返回：

```text
TRANSCRIPT_EMPTY
```

前端可以沿用现有流程，展示可理解的失败提示。

### 2. 文本过短

场景：文本无法形成有效洞察。

后端返回：

```text
TRANSCRIPT_TOO_SHORT
```

建议前端提示用户再说一次。

### 3. 后端处理超时

场景：聆听阶段等待时间过长。

后端返回或前端判定：

```text
AI_TIMEOUT
```

前端不得改变 Ritual 主流程，只能在现有洞察区域给出降级内容。

### 4. 请求过于频繁

场景：同一用户短时间多次请求。

后端返回：

```text
RATE_LIMITED
```

### 5. 生成失败

场景：模型输出格式不合格或服务异常。

后端返回：

```text
INSIGHT_GENERATION_FAILED
```

后端必须保证失败可控，不能把原始错误直接暴露给前端。

## 十、我的心湖大纲

### 1. 历史会话

我的心湖中每条记录需要展示：

```text
会话时间
你真正在纠结的
我注意到的模式
带走的问题
情绪标签
主题标签
收藏状态
反馈状态
删除入口
```

### 2. 最近 7 天

最近 7 天用于展示短期反复出现的问题。

内容包括：

```text
本周会话次数
本周最高频情绪
本周最高频主题
本周重复出现的问题
本周一句总结
```

### 3. 最近 30 天

最近 30 天用于展示更稳定的心绪模式。

内容包括：

```text
本月会话次数
本月最高频情绪
本月最高频主题
常见触发场景
重复表达模式
本月一句总结
下个月可以留意的问题
```

### 4. 收藏的问题

用户可以收藏第三张卡片中的问题。

收藏内容包括：

```text
问题文本
来源会话
创建时间
关联主题标签
```

第一版前端未提供收藏入口时，后端只预留，不强行显示。

## 十一、开发优先级

### 第一优先级

必须先完成：

```text
生成洞察
保存会话
返回三张卡片内容
匿名用户识别
删除全部数据
基础错误处理
基础安全检测
```

### 第二优先级

第一轮可用后完成：

```text
历史列表
7 天模式
30 天模式
用户反馈
单条删除
关闭历史保存
```

### 第三优先级

用户开始连续使用后完成：

```text
收藏问题
导出记录
每周总结
每月总结
长期模式报告
```

## 十二、验收标准

### 1. 前端 UI 验收

必须满足：

```text
页面结构无变化
按钮位置无变化
会话阶段无变化
三张卡片标题无变化
现有动画节奏无变化
现有关闭逻辑无变化
现有再说一次逻辑无变化
现有完成逻辑无变化
```

### 2. 后端结果验收

必须满足：

```text
每次有效会话都返回三份洞察
三份洞察字段完整
每张卡片内容长度适配 UI
结果语气符合产品气质
会话可以保存
历史可以查询
用户可以删除数据
失败时有可控错误
```

### 3. 产品闭环验收

必须跑通：

```text
打开网页
点击开始一次会话
进入入静
点击开始说
完成倾诉
进入聆听
返回真实洞察
展示三张卡片
提示已存入心湖
点击再说一次可重新开始
点击完成可关闭覆盖层
历史中能查到本次会话
删除后历史中不再出现
```

## 十三、禁止事项清单

后端开发过程中禁止出现：

```text
私自增加登录页
私自增加注册页
私自增加付费页
私自增加聊天页
私自增加咨询入口
私自增加用户广场
私自增加分享海报
私自改三张卡片标题
私自改 Ritual 阶段文案
私自改隐私文案
私自改按钮文案
私自改页面结构
私自把错误信息直接显示给用户
私自保存用户音频
私自公开用户内容
私自把用户会话用于展示案例
```

## 十四、交付物

后端第一版应交付：

```text
接口说明
数据结构说明
错误码说明
安全检测说明
隐私数据处理说明
本地测试说明
前后端联调说明
```

前后端联调时，以现有前端 UI 为准。

任何需要改 UI 的地方，必须单独列为产品确认项，不能在后端开发中顺手改掉。

## 十五、最终执行口径

本项目当前选择：

```text
服务器 + 网页版
```

当前前端 UI 已完成。

后端任务是：

```text
让现有 UI 变成真实可用产品
```

后端不得重构产品，不得扩展产品边界，不得新增用户路径。

第一版只完成：

```text
用户说一段话
系统生成三份洞察
本次会话存入心湖
后续能看到 7 天和 30 天模式
用户能删除自己的数据
```

这就是服务器网页版第一版的完整后端大纲。
