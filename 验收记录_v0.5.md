# 心潮服务器网页版 v0.5 本地 .env loader 版

## 目标

零依赖读取 `backend/.env`，便于本机真实 AI 联调。

## 已完成

- 新增 `loadEnv`
- server 启动时自动读取 `backend/.env`
- 终端环境变量优先级高于 `.env`
- 不输出 env value
- env-loader-test 通过
- smoke / persistence / ai fallback / ai provider 继续通过

## 当前默认行为

- 没有 `backend/.env` 时继续使用 `process.env`
- `AI_ENABLED` 默认仍 false
- 不调用真实 AI

## 当前限制

- 还没配置真实 key
- 还没做真实 AI provider 测试
- 正式隐私口径未更新
- 生产部署未做

## 说明

本文档未写入真实 API key，也未写入真实用户内容。
