# 心潮服务器网页版 v0.4 OpenAI compatible provider 版

## 目标

实现真实 OpenAI compatible provider 代码路径，但默认关闭，并通过 fake fetch 测试，不调用真实 AI。

## 已完成

- OpenAI compatible request 构造
- `response_format: { "type": "json_object" }`
- `AbortController` timeout 设计
- AI JSON parse 清洗
- `validateInsightPayload` 校验
- provider 失败 fallback mock
- AI crisis 分支
- fake fetch provider 测试
- smoke / persistence / ai:fallback / ai:provider 测试通过

## 当前默认行为

- `AI_ENABLED=false`
- 不调用真实 AI
- 不发送 transcript 到第三方
- 不需要真实 key
- 接口不变
- 前端不变

## 当前限制

- 尚未使用真实 provider 验证。
- 正式隐私口径未更新。
- `node:sqlite` 仍有 experimental warning。
- 生产部署未做。

## 下一步建议

- 配置真实 provider 做本地人工测试。
- 继续保持 `AI_ENABLED=false` 默认。
- 更新隐私文案。
- 决定是否添加 `SAVE_TRANSCRIPT`。

## 说明

本文档未写入真实 API key，也未写入真实用户 transcript。测试使用的 `dummy_key` 与 `fake.local` 均为假值。
