# 心潮服务器网页版 v0.2 SQLite 持久化版

## 目标

从内存存储升级为 SQLite 持久化存储，让 sessions、tags、feedback、删除状态在服务重启后继续保留。

## 已实现

- `node:sqlite` 初始化
- `sessions` 表
- `session_tags` 表
- `session_feedback` 表
- `sqliteStore` 替换 `memoryStore`
- 接口路径保持不变
- 接口返回结构保持不变
- smoke test 已通过
- 重启后数据保留验证已完成

## 当前限制

- `node:sqlite` 仍可能出现 experimental warning。
- 未接 AI。
- 未接正式数据库服务。
- 未做生产部署。
- 未做正式隐私口径调整。

## 下一步建议

- 增加持久化专用 smoke test。
- 接 AI 洞察生成。
- 调整正式隐私口径。
- 决定是否默认启用后端模式。
